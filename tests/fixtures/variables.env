# Test variable expansion
BASE=/usr/local
BIN_DIR=${BASE}/bin
LIB_DIR=${BASE}/lib

# Simple $VAR syntax
HOME_VAR=/home/user
USER_BIN=$HOME_VAR/bin

# Mixed syntax
FULL_PATH=${BASE}/share:$HOME_VAR/local

# Reference later-defined var (should work in second pass)
DERIVED=${TARGET}
TARGET=derived_value

# System environment (fallback to actual USER env var if set)
CURRENT_USER=$USER

# Undefined variable (keeps literal)
UNDEFINED=${DOES_NOT_EXIST}
